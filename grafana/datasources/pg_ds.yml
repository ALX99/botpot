apiVersion: 1

datasources:
  - name: HoneypotDB
    type: postgres
    url: db:5432
    database: postgres
    user: grafana
    isDefault: true
    secureJsonData:
      password: example
    jsonData:
      sslmode: disable
      postgresVersion: 10
      timescaledb: false
